{
  "name": "cloudflare-worker-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker style proxy service",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "pm2-start": "pm2 start server.js --name worker --max-memory-restart 300M --cron-restart=\"0 */6 * * *\"",
    "pm2-monitor": "pm2 monit",
    "pm2-logs": "pm2 logs --timestamp --lines 100",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
